import { Spinner } from '../../../components/Spinner';
import { Content } from '../../../components/styles';

export const LoadingPage = () => {
  return (
    <Content>
      <Spinner />
    </Content>
  );
};
